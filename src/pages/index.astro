---
import Layout from '../layouts/Layout.astro';
---

<Layout>
	<main>
		<div class="grid">
			<header>
				<div class="circle"></div>
				<h1>Yuriy</h1>
				<p class="role">Tech Lead</p>
			</header>

			<section class="about">
				<p>Engineer with 15+ years building web stuff. I like writing code that works, ships fast, and doesn't make me sad when I have to change it later.</p>
			</section>

			<section class="focus">
				<h2>Currently Into</h2>
				<p>Edge computing, TypeScript, and modern web infrastructure. AI accelerated coding. Localization and internationalization.</p>
			</section>

			<div class="block-accent"></div>
		</div>
	</main>
</Layout>

<style>
	main {
		min-height: 100vh;
		background: #f5f5f0;
		font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
		color: #1a1a1a;
	}

	.grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		max-width: 900px;
		margin: 0 auto;
		padding: 6rem 3rem;
	}

	header {
		grid-column: 1 / -1;
		position: relative;
		margin-bottom: 4rem;
		padding-left: 80px;
	}

	.circle {
		position: absolute;
		left: 0;
		top: 50%;
		transform: translateY(-50%);
		width: 56px;
		height: 56px;
		background: #1a1a1a;
		border-radius: 50%;
	}

	h1 {
		font-size: 4.5rem;
		font-weight: 700;
		margin: 0;
		line-height: 1;
		letter-spacing: -0.03em;
	}

	.role {
		font-size: 1rem;
		margin: 0.75rem 0 0;
		text-transform: uppercase;
		letter-spacing: 0.15em;
		font-weight: 400;
	}

	.about {
		grid-column: 1 / -1;
		border-top: 2px solid #1a1a1a;
		padding: 2rem 0;
		max-width: 520px;
	}

	.focus {
		grid-column: 1 / -1;
		border-top: 2px solid #1a1a1a;
		padding: 2rem 0;
		max-width: 520px;
	}

	h2 {
		font-size: 0.75rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.2em;
		margin: 0 0 1rem;
	}

	p {
		margin: 0;
		font-size: 1.25rem;
		line-height: 1.6;
	}

	.block-accent {
		grid-column: 2;
		justify-self: end;
		align-self: end;
		width: 48px;
		height: 48px;
		background: #1a1a1a;
		margin-top: 3rem;
	}

	@media (max-width: 640px) {
		.grid {
			grid-template-columns: 1fr;
			padding: 3rem 1.5rem;
		}

		header {
			padding-left: 0;
			padding-top: 70px;
		}

		.circle {
			top: 0;
			transform: none;
			width: 48px;
			height: 48px;
		}

		h1 {
			font-size: 3rem;
		}

		.block-accent {
			grid-column: 1;
			justify-self: start;
		}
	}
</style>
